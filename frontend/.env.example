# Origin of the SEWA backend API.
# Must match the backend's CLIENT_ORIGIN pairing: the backend locks CORS to
# a single origin with credentials enabled, so these two have to agree.
VITE_API_URL=http://localhost:4000