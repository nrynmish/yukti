diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/README.md yukti/README.md
--- orig/yukti/README.md	2026-09-12 15:51:25.885702812 +0000
+++ yukti/README.md	2026-09-12 15:52:30.747550991 +0000
@@ -77,6 +77,7 @@
 OTP_EXPIRY_MINUTES=10
 OTP_MAX_ATTEMPTS=5
 OTP_RESEND_COOLDOWN_SECONDS=60
+OTP_CLEANUP_RETENTION_MINUTES=20
 
 SMTP_HOST=...
 SMTP_PORT=587
@@ -182,14 +183,21 @@
 POST /api/auth/password/forgot
 POST /api/auth/password/reset
 
-POST   /api/register                          create team (leader = current user)
+GET  /api/profile                              current user + candidate profile
+PUT  /api/profile                              upsert the "Personal Details" step
+
+POST   /api/register                           create team (leader = current user)
 GET    /api/register/me                        current user's team + members
+PATCH  /api/register/:teamId                   edit a draft team (409 once submitted)
 POST   /api/register/:teamId/members           add a member
 DELETE /api/register/:teamId/members/:memberId remove a member
 POST   /api/register/:teamId/submit            lock and submit
+
+GET  /api/health                               liveness + database connectivity
 ```
 
-All `/api/register/*` routes require a signed-in, email-verified user.
+All `/api/register/*` and `/api/profile` routes require a signed-in,
+email-verified user.
 
 ## Known gaps
 
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/backend/README.md yukti/backend/README.md
--- orig/yukti/backend/README.md	2026-09-12 15:51:25.885778807 +0000
+++ yukti/backend/README.md	2026-09-12 15:52:30.747349696 +0000
@@ -6,21 +6,30 @@
 ## Flow
 
 ```
-POST /api/auth/signup        -> create account (unverified)
-POST /api/auth/otp/send      -> (re)send email OTP
-POST /api/auth/otp/verify    -> verify OTP, activates account, sets session cookie
-POST /api/auth/signin        -> sign in, sets session cookie
-POST /api/auth/signout       -> clear session
-GET  /api/auth/me            -> current user
-
-POST   /api/register              -> create team (leader = current user)
-GET    /api/register/me           -> current user's team + members
-POST   /api/register/:teamId/members         -> add a member (no account required)
+POST /api/auth/signup           -> create account (unverified)
+POST /api/auth/otp/send         -> (re)send email OTP
+POST /api/auth/otp/verify       -> verify OTP, activates account, sets session cookie
+POST /api/auth/signin           -> sign in, sets session cookie
+POST /api/auth/signout          -> clear session
+GET  /api/auth/me               -> current user
+POST /api/auth/password/forgot  -> send a password-reset OTP
+POST /api/auth/password/reset   -> consume the OTP, set a new password
+
+GET  /api/profile               -> current user + candidate profile (null until saved)
+PUT  /api/profile               -> full-replace upsert of the "Personal Details" step
+
+POST   /api/register                           -> create team (leader = current user)
+GET    /api/register/me                        -> current user's team + members
+PATCH  /api/register/:teamId                   -> edit a draft team's name/institute/theme/PS
+POST   /api/register/:teamId/members           -> add a member (no account required)
 DELETE /api/register/:teamId/members/:memberId -> remove a member
-POST   /api/register/:teamId/submit           -> lock and submit the team
+POST   /api/register/:teamId/submit            -> lock and submit the team
+
+GET  /api/health                -> liveness + database connectivity
 ```
 
-All `/api/register/*` routes require a signed-in, **email-verified** user.
+All `/api/register/*` and `/api/profile` routes require a signed-in,
+**email-verified** user.
 
 ## Setup
 
@@ -48,9 +57,6 @@
 
 ## What's deliberately NOT here yet
 
-- **Password reset flow** — the `OtpPurpose.password_reset` enum value and
-  `AppError`/audit scaffolding are in place, but the endpoints aren't wired
-  up. Same OTP service handles both purposes already.
 - **Rate limiter store** — uses in-memory `express-rate-limit`. If you
   deploy more than one instance behind a load balancer, swap in
   `rate-limit-redis` (limits won't be shared across processes otherwise).
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/backend/src/controllers/team.controller.ts yukti/backend/src/controllers/team.controller.ts
--- orig/yukti/backend/src/controllers/team.controller.ts	2026-09-12 15:51:25.886532958 +0000
+++ yukti/backend/src/controllers/team.controller.ts	2026-09-12 15:52:15.069948216 +0000
@@ -36,6 +36,12 @@
 
 export async function removeMember(req: Request, res: Response) {
   await teamService.removeTeamMember(req.params.teamId!, req.user!.id, req.params.memberId!);
+  await writeAuditLog({
+    req,
+    userId: req.user!.id,
+    action: "team_member_remove",
+    metadata: { teamId: req.params.teamId, memberId: req.params.memberId },
+  });
   res.status(204).send();
 }
 
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/backend/src/lib/audit.ts yukti/backend/src/lib/audit.ts
--- orig/yukti/backend/src/lib/audit.ts	2026-09-12 15:51:25.886569915 +0000
+++ yukti/backend/src/lib/audit.ts	2026-09-12 15:52:15.069870757 +0000
@@ -17,6 +17,7 @@
   | "team_create"
   | "team_update"
   | "team_member_add"
+  | "team_member_remove"
   | "team_submit"
   | "profile_update";
 
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/backend/src/routes/index.ts yukti/backend/src/routes/index.ts
--- orig/yukti/backend/src/routes/index.ts	2026-09-12 15:51:25.886867384 +0000
+++ yukti/backend/src/routes/index.ts	2026-09-12 15:52:15.070150323 +0000
@@ -3,6 +3,7 @@
 import { teamRouter } from "./team.routes.js";
 import { profileRouter } from "./profile.routes.js";
 import { prisma } from "../config/prisma.js";
+import { logger } from "../config/logger.js";
 
 export const apiRouter = Router();
 
@@ -19,8 +20,8 @@
       status: "OK",
       message: "SEWA 2026 backend and database are connected!",
     });
-  } catch (error) {
-    console.error("Health check failed:", error);
+  } catch (err) {
+    logger.error({ err }, "health_check_failed");
 
     res.status(500).json({
       status: "ERROR",
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/backend/src/schemas/auth.schema.ts yukti/backend/src/schemas/auth.schema.ts
--- orig/yukti/backend/src/schemas/auth.schema.ts	2026-09-12 15:51:25.886953622 +0000
+++ yukti/backend/src/schemas/auth.schema.ts	2026-09-12 15:52:15.069516570 +0000
@@ -1,7 +1,5 @@
 import { z } from "zod";
-
-// Indian mobile number: optional +91, then a 10-digit number starting 6-9.
-const phoneRegex = /^(\+91)?[6-9]\d{9}$/;
+import { phoneSchema } from "./phone.js";
 
 // At least one lowercase, one uppercase, one digit, one special char.
 const passwordRegex = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[^A-Za-z0-9]).+$/;
@@ -11,7 +9,7 @@
     firstName: z.string().trim().min(1).max(100),
     lastName: z.string().trim().min(1).max(100),
     email: z.string().trim().toLowerCase().email().max(255),
-    phone: z.string().trim().regex(phoneRegex, "Invalid Indian mobile number").optional(),
+    phone: phoneSchema.optional(),
     password: z
       .string()
       .min(8, "Password must be at least 8 characters")
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/backend/src/schemas/phone.ts yukti/backend/src/schemas/phone.ts
--- orig/yukti/backend/src/schemas/phone.ts	1970-01-01 00:00:00.000000000 +0000
+++ yukti/backend/src/schemas/phone.ts	2026-09-12 15:48:21.364252713 +0000
@@ -0,0 +1,20 @@
+import { z } from "zod";
+
+/** Indian mobile number: optional +91, then a 10-digit number starting 6-9. */
+export const phoneRegex = /^(\+91)?[6-9]\d{9}$/;
+
+/**
+ * The one phone rule for the whole API.
+ *
+ * Every form in the portal that takes a phone number shows (or suggests) the
+ * readable spaced format "+91 98765 43210", so whitespace is stripped before
+ * the shape is checked. This lives here rather than being redeclared per
+ * schema because the three copies had drifted: the profile endpoint stripped
+ * whitespace while signup and add-member did not, so the exact string the UI
+ * suggests was accepted on one screen of the registration dossier and
+ * rejected on another.
+ */
+export const phoneSchema = z
+  .string()
+  .transform((v) => v.replace(/\s+/g, ""))
+  .pipe(z.string().regex(phoneRegex, "Invalid Indian mobile number"));
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/backend/src/schemas/profile.schema.ts yukti/backend/src/schemas/profile.schema.ts
--- orig/yukti/backend/src/schemas/profile.schema.ts	2026-09-12 15:51:25.886981840 +0000
+++ yukti/backend/src/schemas/profile.schema.ts	2026-09-12 15:52:15.069709353 +0000
@@ -1,6 +1,6 @@
 import { z } from "zod";
+import { phoneSchema } from "./phone.js";
 
-const phoneRegex = /^(\+91)?[6-9]\d{9}$/;
 const aadhaarRegex = /^\d{12}$/;
 const pinRegex = /^\d{6}$/;
 const dobRegex = /^\d{4}-\d{2}-\d{2}$/;
@@ -15,15 +15,6 @@
   "Chandigarh",
 ] as const;
 
-// The registration form's phone placeholders show a spaced-out format
-// ("+91 98765 43210") for readability, so accept that on input rather than
-// rejecting a value the UI itself suggested — strip whitespace before
-// checking shape.
-const phoneSchema = z
-  .string()
-  .transform((v) => v.replace(/\s+/g, ""))
-  .pipe(z.string().regex(phoneRegex, "Invalid Indian mobile number"));
-
 // Category / nationality / gender / state are kept as free text rather than
 // enums — same rationale as team.schema's theme/problemStatement: these
 // lists are expected to be finalized before launch and a DB enum would need
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/backend/src/schemas/team.schema.ts yukti/backend/src/schemas/team.schema.ts
--- orig/yukti/backend/src/schemas/team.schema.ts	2026-09-12 15:51:25.887005467 +0000
+++ yukti/backend/src/schemas/team.schema.ts	2026-09-12 15:52:15.069602163 +0000
@@ -1,6 +1,5 @@
 import { z } from "zod";
-
-const phoneRegex = /^(\+91)?[6-9]\d{9}$/;
+import { phoneSchema } from "./phone.js";
 
 // Adjust MIN/MAX to SEWA 2026's actual team-size rules.
 export const TEAM_MIN_MEMBERS = 2; // leader + at least 1 other
@@ -24,7 +23,7 @@
     firstName: z.string().trim().min(1).max(100),
     lastName: z.string().trim().min(1).max(100),
     email: z.string().trim().toLowerCase().email(),
-    phone: z.string().trim().regex(phoneRegex, "Invalid Indian mobile number").optional(),
+    phone: phoneSchema.optional(),
   })
   .strict();
 
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/backend/src/services/cleanup.service.ts yukti/backend/src/services/cleanup.service.ts
--- orig/yukti/backend/src/services/cleanup.service.ts	2026-09-12 15:51:25.887071964 +0000
+++ yukti/backend/src/services/cleanup.service.ts	2026-09-12 15:52:15.070071158 +0000
@@ -1,5 +1,6 @@
 import { prisma } from '../config/prisma.js';
 import { env } from '../config/env.js';
+import { logger } from '../config/logger.js';
 import cron from 'node-cron';
 
 /**
@@ -21,11 +22,8 @@
     },
   });
 
-  // Optional: log for observability
   if (result.count > 0) {
-    console.info(
-      `[OTP Cleanup] Deleted ${result.count} OTP rows older than ${retentionMinutes} min`
-    );
+    logger.info({ deleted: result.count, retentionMinutes }, 'otp_cleanup_deleted');
   }
 }
 
@@ -35,11 +33,10 @@
 export function startOtpCleanupJob(): void {
   // Runs every 5 minutes; adjust the schedule if you want more/less frequent runs.
   cron.schedule('*/5 * * * *', () => {
-    console.info('[OTP Cleanup] Running scheduled cleanup…');
     cleanupOldOtps().catch((err) => {
-      console.error('[OTP Cleanup] Error during cleanup:', err);
+      logger.error({ err }, 'otp_cleanup_failed');
     });
   });
 
-  console.info('[OTP Cleanup] Scheduled job started (runs every 5 minutes).');
+  logger.info('otp_cleanup_scheduled');
 }
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/backend/src/services/profile.service.ts yukti/backend/src/services/profile.service.ts
--- orig/yukti/backend/src/services/profile.service.ts	2026-09-12 15:51:25.887127697 +0000
+++ yukti/backend/src/services/profile.service.ts	2026-09-12 15:52:15.069791811 +0000
@@ -40,8 +40,19 @@
     backupEmail: backupEmail ?? null,
   };
 
-  const [, profile] = await prisma.$transaction([
+  const [, , profile] = await prisma.$transaction([
     prisma.user.update({ where: { id: userId }, data: { firstName, lastName, phone } }),
+    // The leader's team_members row is copied from User at team-creation time
+    // and is never re-sent by the client (the wizard treats roster slot 0 as
+    // display-only). Without this, correcting your name or phone in Step 1
+    // after a draft team already exists leaves the stored roster showing what
+    // you signed up with, while the reviewed and printed confirmation shows
+    // the corrected values. Scoped to draft teams so a submitted roster stays
+    // locked.
+    prisma.teamMember.updateMany({
+      where: { userId, role: "leader", team: { status: "draft" } },
+      data: { firstName, lastName, phone },
+    }),
     prisma.candidateProfile.upsert({
       where: { userId },
       create: { userId, ...profileData },
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/frontend/src/components/TeamRegisterPage.tsx yukti/frontend/src/components/TeamRegisterPage.tsx
--- orig/yukti/frontend/src/components/TeamRegisterPage.tsx	2026-09-12 15:51:25.888685765 +0000
+++ yukti/frontend/src/components/TeamRegisterPage.tsx	2026-09-12 15:52:15.069358848 +0000
@@ -150,23 +150,45 @@
 
 const emptyMember = (): MemberDraft => ({ firstName: "", lastName: "", email: "", phone: "" });
 
+// These mirror the server's schemas (backend/src/schemas/phone.ts plus
+// profile.schema.ts / team.schema.ts). The wizard's gates used to check only
+// that a field was non-empty, so a 5-digit Aadhaar or a 2-character team name
+// passed "Next" and then came back as a 400 from the endpoint. Keeping the
+// shapes in sync means the button is disabled for exactly the input the
+// server would reject. The server remains the real validator.
+const PHONE_RE = /^(\+91)?[6-9]\d{9}$/;
+const AADHAAR_RE = /^\d{12}$/;
+const PIN_RE = /^\d{6}$/;
+const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
+
+/** Whitespace is stripped first, matching the server's phone schema. */
+const phoneOk = (v: string) => PHONE_RE.test(v.replace(/\s+/g, ""));
+
+const TEAM_NAME_MIN = 3; // createTeamSchema.name.min(3)
+const INSTITUTE_MIN = 2; // createTeamSchema.institute.min(2)
+
 const personalComplete = (p: PersonalDraft) =>
   !!(
-    p.firstName &&
-    p.lastName &&
+    p.firstName.trim() &&
+    p.lastName.trim() &&
     p.nationality &&
     p.dateOfBirth &&
     p.gender &&
-    p.aadhaarNumber &&
-    p.addressLine1 &&
-    p.pinCode &&
-    p.city &&
     p.state &&
-    p.country &&
-    p.phone
-  );
-
-const memberComplete = (m: MemberDraft) => !!(m.firstName && m.lastName && m.email);
+    p.country
+  ) &&
+  p.addressLine1.trim().length >= 3 &&
+  p.city.trim().length >= 2 &&
+  AADHAAR_RE.test(p.aadhaarNumber) &&
+  PIN_RE.test(p.pinCode) &&
+  phoneOk(p.phone) &&
+  (!p.alternatePhone.trim() || phoneOk(p.alternatePhone)) &&
+  (!p.backupEmail.trim() || EMAIL_RE.test(p.backupEmail.trim()));
+
+const memberComplete = (m: MemberDraft) =>
+  !!(m.firstName.trim() && m.lastName.trim()) &&
+  EMAIL_RE.test(m.email.trim()) &&
+  (!m.phone.trim() || phoneOk(m.phone));
 
 /** First duplicate email among members (leader included), or null. */
 function findDuplicateEmail(members: MemberDraft[]): string | null {
@@ -456,7 +478,7 @@
 // ─── Page ────────────────────────────────────────────────────────────────────
 
 export function TeamRegisterPage() {
-  const { user } = useAuth();
+  const { user, refresh } = useAuth();
 
   const [step, setStep] = useState<WizardStep>(1);
   const [personal, setPersonal] = useState<PersonalDraft>(emptyPersonal());
@@ -637,6 +659,10 @@
             : m,
         ),
       );
+      // Step 1 writes firstName/lastName/phone back to the User record, so the
+      // cached `me` query is now stale -- without this the header and any other
+      // consumer keep showing the pre-edit name until the 30s staleTime lapses.
+      await refresh();
       return true;
     } catch (err) {
       setError(
@@ -825,7 +851,10 @@
             </div>
           </div>
 
-          <div className="hidden">
+          {/* Hidden on screen but still laid out when printing. `hidden`
+              (display:none) would keep this out of the print layout too, so
+              "Download / Print Confirmation" produced a blank page. */}
+          <div className="print-only">
             <ConfirmationSummary
               personal={personal}
               email={user?.email ?? ""}
@@ -1200,7 +1229,7 @@
               <div className="space-y-7 px-6 py-8 sm:px-8">
                 <SectionHeader n={1} icon={IdCard} title="Team Identity" />
                 <div className="-mt-5 grid grid-cols-1 gap-4 sm:grid-cols-2">
-                  <Field label="Team Name" required>
+                  <Field label="Team Name" required hint="At least 3 characters.">
                     <input
                       className={inputClass}
                       value={teamName}
@@ -1288,7 +1317,11 @@
                     setError("");
                     setStep(4);
                   }}
-                  nextDisabled={!teamName || !institute || members.some((m) => !memberComplete(m))}
+                  nextDisabled={
+                    teamName.trim().length < TEAM_NAME_MIN ||
+                    institute.trim().length < INSTITUTE_MIN ||
+                    members.some((m) => !memberComplete(m))
+                  }
                 />
               </div>
             )}
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/frontend/src/lib/api.ts yukti/frontend/src/lib/api.ts
--- orig/yukti/frontend/src/lib/api.ts	2026-09-12 15:51:25.890482159 +0000
+++ yukti/frontend/src/lib/api.ts	2026-09-12 15:52:15.068546965 +0000
@@ -10,7 +10,7 @@
  *    `CLIENT_ORIGIN` must agree about the pair of origins in play.
  */
 
-const API_URL = (import.meta.env.VITE_API_URL ?? "http://localhost:4000").replace(/\/$/, "");
+const API_URL = (import.meta.env["VITE_API_URL"] ?? "http://localhost:4000").replace(/\/$/, "");
 
 /** A non-2xx response. `details` carries Zod's per-field errors when present. */
 export class ApiError extends Error {
@@ -65,8 +65,14 @@
   return body as T;
 }
 
+// `body` is omitted entirely rather than passed as `undefined`: under
+// `exactOptionalPropertyTypes`, RequestInit.body is `BodyInit | null` and
+// will not accept an explicit `undefined`.
 const post = <T>(path: string, body?: unknown) =>
-  request<T>(path, { method: "POST", body: body ? JSON.stringify(body) : undefined });
+  request<T>(
+    path,
+    body === undefined ? { method: "POST" } : { method: "POST", body: JSON.stringify(body) },
+  );
 
 // ─── Types ──────────────────────────────────────────────────────────────────
 
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/frontend/src/lib/auth.tsx yukti/frontend/src/lib/auth.tsx
--- orig/yukti/frontend/src/lib/auth.tsx	2026-09-12 15:51:25.890517464 +0000
+++ yukti/frontend/src/lib/auth.tsx	2026-09-12 15:52:15.068875608 +0000
@@ -85,8 +85,11 @@
           <p className="mt-2 text-sm text-gray-500">
             Team registration opens once your email address is confirmed.
           </p>
+          {/* /signin, not /signup: the account already exists, so re-submitting
+              the signup form just 409s. Signing in with an unverified account
+              returns 403, which AuthPage turns into the OTP screen. */}
           <Link
-            to="/signup"
+            to="/signin"
             className="mt-5 inline-flex h-10 items-center rounded-md bg-[#ff5a5f] px-5 text-sm font-semibold text-white"
           >
             Verify now
diff -ruN '--exclude=node_modules' '--exclude=package-lock.json' '--exclude=deploy' orig/yukti/frontend/src/styles.css yukti/frontend/src/styles.css
--- orig/yukti/frontend/src/styles.css	2026-09-12 15:51:25.887989418 +0000
+++ yukti/frontend/src/styles.css	2026-09-12 15:52:15.068762488 +0000
@@ -857,13 +857,25 @@
  * step — `.print-area` is the only thing left visible when the browser's
  * print dialog opens, so "Print" doubles as "Save as PDF" with no extra
  * dependency. `.no-print` hides chrome (nav, sidebar, buttons) that has no
- * business on a printed page.
+ * business on a printed page, and `.print-only` is the inverse: absent on
+ * screen, present when printing. `.print-only` must NOT be `display: none`
+ * on screen and merely made `visible` in print -- a `display: none` ancestor
+ * removes its subtree from layout entirely, so `visibility: visible` on the
+ * descendants prints nothing at all.
  */
+.print-only {
+  display: none;
+}
+
 @media print {
   body * {
     visibility: hidden;
   }
 
+  .print-only {
+    display: block;
+  }
+
   .print-area,
   .print-area * {
     visibility: visible;
